//Paul Kwak

/* Polar Equations
x = rcos(theta)
y = rsin(theta)
r^2 = (x^2 + y^2)

Displays the point's corresponding polar coordinates
The quadrant that the point is in
The axis it is on, or the origin
*/

#include <iostream>
#include <cmath>
using namespace std;

//prototyping functions

int main()
{
  cout << "x-coordinate: " << endl;
  cin >> x;
  cout << "y-coordinate: " << endl;
  cin >> y;

  //polar function used

}

int getPolar(int x, int y)
{
  int r;
  r.^2 = x.^2 + y.^2;
  int theta;
  theta = atan(y/x);
  x = rcos(theta)
}
